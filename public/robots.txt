# OASIS VISION — OASIS AI
# https://oasisai.work
#
# The Sitemap host below must match SITE_URL in src/app/layout.tsx. A sitemap
# announced on a different host than it lives on is silently ignored.

User-agent: *
Allow: /
Disallow: /api/
Disallow: /api/scanner
Disallow: /api/osint/*

# Allow search engines to index the main page
Allow: /api/health

# Sitemap
Sitemap: https://oasisai.work/sitemap.xml

# Crawl-delay for respectful indexing
Crawl-delay: 10
